<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core"
         xmlns:f="/lib/form">
    <f:section title="HA Jenkins Configuration">
        <f:entry title="Should serve builds?" field="serveBuilds">
            <f:checkbox checked="${it.serveBuilds}"/>
        </f:entry>
        <f:entry title="Redis host" field="redisHost">
            <f:textbox/>
        </f:entry>
    </f:section>
</j:jelly>
